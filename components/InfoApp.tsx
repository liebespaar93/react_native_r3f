import React from 'react'
import TextTutorial from './tutorial/TextTutorial'

function InfoApp() {
  return (
    <TextTutorial>
      {`본문 내용입니다.\n` +
        `이러한 react native 튜토리얼을 진행하고 있습니다.\n` +
        `여러가지 명령어를 습득하고 있으며 추후 r3f를 적용할 예정입니다.\n ` +
        `많은 관심을 부탁드립니다.\n` +
        `목차 \n` +
        `- view\n` +
        `- text\n` +
        `- image\n` +
        `- scroll view\n` +
        `- button\n` +
        `- pressable\n` +
        `- modal\n` +
        `- status bar\n` +
        `- activity indicator\n` +
        `- alert\n` +
        `- custom components\n` +
        `- styling\n` +
        `- stylesheet api\n` +
        `- mutiple styles\n` +
        `- box model\n` +
        `- shadow and elevation\n` +
        `- style inheritance\n` +
        `- layout with flexbox\n` +
        `- code setup\n` +
        `- flex\n` +
        `- flex direction\n` +
        `- justify content\n` +
        `- align content\n` +
        `- align self\n` +
        `- flex wrap\n` +
        `- align content\n` +
        `- gap\n` +
        `- flex basis\n` +
        `- flex grow\n` +
        `- realative and absolute layout\n` +
        `- dynamic user interfaces\n` +
        `- dimension api\n` +
        `- dimaensions api drawback\n` +
        `- usewindowdimensions\n` +
        `- safeareaview\n` +
        `- platform specific code\n` +
        `- build a pokemon card(1/6)\n` +
        `- build a pokemon card(2/6)\n` +
        `- build a pokemon card(3/6)\n` +
        `- build a pokemon card(4/6)\n` +
        `- build a pokemon card(5/6)\n` +
        `- build a pokemon card(6/6)\n` +
        `- lists\n` +
        `- flatlist\n` +
        `- item separator\n` +
        `- list empty\n` +
        `- list header and footer\n` +
        `- section list\n` +
        `- innputs and forms\n` +
        `- textinput\n` +
        `- textinput props\n` +
        `- multiline textinput\n` +
        `- switch\n` +
        `- login form\n` +
        `- keyboardaviodingview\n` +
        `- form validation\n` +
        `- form submission\n` +
        `- networking\n` +
        `- get requests\n` +
        `- loading state\n` +
        `- pull to refresh\n` +
        `- post request\n` +
        `- error handling\n` +
        `- navigation\n` +
        `- stack navigation\n` +
        `- navigation between screens\n` +
        `- passing data between screens\n` +
        `- stack navigation options\n` +
        `- dynamic stack navigator\n` +
        `- drawer navigation\n` +
        `- tab navigation\n` +
        `- tab navigation options\n` +
        `- nesting navigators\n`
      }
    </TextTutorial>
  )
}

export default InfoApp